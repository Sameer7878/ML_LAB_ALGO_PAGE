import React from "react";

const Chooseoption = (props) => {
    const name = props.name
    return (
        <div>
        <h1>Upload File Name: {name}</h1>
        </div>
    );
}

export default Chooseoption;